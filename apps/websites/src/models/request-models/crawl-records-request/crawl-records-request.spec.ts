import { CrawlRecordsRequest } from './crawl-records-request';

describe('CrawlRecordsRequest', () => {
  it('should be defined', () => {
    expect(new CrawlRecordsRequest()).toBeDefined();
  });
});
